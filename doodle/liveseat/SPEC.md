**'공연 예매 시스템 (Ticket Reservation System)'** 시나리오를 추천합니다.

이 시나리오는 게임 서버의 '인벤토리/아이템 관리'와 유사한 **동시성 제어(Concurrency Control)** 이슈가 포함되어 있어, 기존 경험과 연결하며 학습하기에 아주 적합합니다.

---

## 🎟️ 프로젝트 명: LiveSeat (온라인 공연 예매 플랫폼)

### 1. 시나리오 개요
사용자는 웹사이트에 접속하여 현재 예매 가능한 공연 목록을 확인하고, 원하는 날짜와 좌석을 선택하여 예매합니다. 관리자는 공연 정보를 등록하고 예매 현황을 관리합니다.

**이 프로젝트가 좋은 이유:**
* **데이터의 상태 변경:** '예약 가능 -> 예약 중 -> 예약 완료'의 상태 변화를 다룹니다.
* **폼 데이터 처리:** 사용자 입력 값(좌석, 날짜 등)을 검증(Validation)하는 로직이 필수적입니다.
* **조회 로직:** 페이징(Pagination)과 검색 기능을 구현하며 쿼리 처리를 익힐 수 있습니다.

---

## 📋 유스케이스 리스트 (Use Cases)

기능을 **사용자(User)**와 **관리자(Admin)**로 나누어 Spring MVC의 어떤 기술을 익힐 수 있는지 함께 정리했습니다.

### [Actor: 일반 사용자]

#### 1. 공연 목록 조회 (Concert List)
* **기능:** 현재 예매 가능한 공연의 리스트를 썸네일, 제목, 날짜와 함께 카드 형태로 봅니다.
* **학습 포인트:**
    * `@GetMapping`을 이용한 조회 처리
    * `Pageable`을 이용한 **페이징(Pagination)** 및 정렬 구현
    * Model 객체에 데이터를 담아 View(Thymeleaf/JSP)로 전달하는 흐름

#### 2. 공연 상세 정보 확인 (Concert Detail)
* **기능:** 특정 공연을 클릭하여 상세 설명, 가격, 잔여 좌석 수를 확인합니다.
* **학습 포인트:**
    * `@PathVariable`을 통한 URL 파라미터 매핑 (`/concerts/{id}`)
    * DTO(Data Transfer Object)와 Entity의 분리



[Image of Spring MVC request lifecycle]


#### 3. 좌석 선택 및 예매 요청 (Booking Request)
* **기능:** 원하는 날짜와 좌석 등급(VIP/R/S)을 선택하고 예매 버튼을 누릅니다.
* **학습 포인트:**
    * `@PostMapping`을 이용한 데이터 생성
    * `@Valid`와 `BindingResult`를 이용한 **입력값 유효성 검사** (예: 과거 날짜 선택 불가, 음수 인원 불가)
    * **핵심:** `@Transactional`을 사용하여 결제/예약 데이터의 정합성 보장

#### 4. 예매 내역 조회 및 취소 (My Page)
* **기능:** 내가 예매한 내역을 확인하고, 필요시 예매를 취소합니다.
* **학습 포인트:**
    * Session 혹은 간단한 Cookie 기반의 사용자 식별 (Security 도입 전 단계)
    * 데이터 수정/삭제(`Soft Delete` 권장) 로직 구현

---

### [Actor: 관리자]

#### 1. 공연 등록 (Register Concert)
* **기능:** 공연 포스터 이미지, 제목, 설명, 가격, 공연 기간을 입력하여 시스템에 등록합니다.
* **학습 포인트:**
    * `MultipartFile`을 이용한 **파일 업로드** 처리
    * 날짜/시간 데이터 타입(`LocalDateTime`) 핸들링

#### 2. 예매 현황 대시보드 (Dashboard)
* **기능:** 특정 공연의 총 좌석 수 대비 예매된 좌석 수를 확인합니다.
* **학습 포인트:**
    * 복잡한 비즈니스 로직을 Service 계층으로 분리하여 **클린 코드** 작성
    * JPQL 혹은 QueryDSL을 이용한 통계 쿼리 작성

---

## 🛠️ 기술 스택 및 아키텍처 제안

기존에 선호하시는 **'실용적인 코드'**와 **'불필요한 추상화 지양'** 원칙에 맞춰 아래와 같이 구성을 제안합니다.

* **Language:** Java 17 or 21
* **Framework:** Spring Boot 3.x (Spring MVC 포함)
* **Database:** H2 (개발용) -> MySQL (운영용 고려)
* **Persistence:** Spring Data JPA (MyBatis보다 객체지향적인 접근 추천)
* **View:** Thymeleaf (SSR 방식, 백엔드 로직 확인에 용이)



